/**
 * PLACEHOLDER — será substituído no Bloco 1 pelos tipos gerados via CLI:
 *   supabase gen types typescript --local > packages/core/types/supabase.ts
 * Não escreva tipo de tabela à mão neste arquivo (CLAUDE.md §7).
 */
export type Database = Record<string, never>;
